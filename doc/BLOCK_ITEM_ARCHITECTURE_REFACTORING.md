# Рефакторинг архитектуры блоков, предметов и инструментов

**Дата создания:** 22 января 2026  
**Статус:** В планировании

---

## 🎯 Цель

Создать модульную, расширяемую архитектуру для блоков, предметов и инструментов, которая упростит добавление нового контента в игру.

---

## 📊 Анализ текущей архитектуры

### Проблемы текущей реализации

#### 1. **Разрозненные данные**
Характеристики одного блока/предмета разбросаны по множеству файлов:

**Пример: GRASS (трава)**
- `src/constants/Blocks.ts` - ID блока (`GRASS: 1`)
- `src/constants/BlockColors.ts` - цвета для рендеринга
- `src/constants/BlockNames.ts` - название для UI
- `src/world/World.ts` - время ломания (`getBreakTime()`)
- `src/world/generation/TextureAtlas.ts` - текстура (процедурная генерация)
- `src/crafting/Recipes.ts` - рецепты крафта (если есть)

**Проблема:** Чтобы добавить новый блок, нужно редактировать 5-7 файлов в разных папках.

#### 2. **Хардкод характеристик**
Все характеристики прописаны в коде через `switch/case`:

```typescript
// src/world/World.ts
public getBreakTime(blockType: number, toolId: number = 0): number {
  switch (blockType) {
    case BLOCK.GRASS:
      if (toolId === BLOCK.IRON_SHOVEL) time = 100;
      else if (toolId === BLOCK.STONE_SHOVEL) time = 200;
      // ...
  }
}
```

**Проблема:** Нельзя добавить блок без изменения кода. Невозможно загрузить блоки из модов.

#### 3. **Процедурная генерация текстур**
Текстуры генерируются в коде через `TextureAtlas.ts`:

```typescript
// Slot 6: Coal Ore (96-112)
if (x >= 96 && x < 112) {
  // Рисуем пиксели вручную
  data[stride] = rgb.r;
  data[stride + 1] = rgb.g;
  // ...
}
```

**Проблема:** 
- Сложно добавлять новые текстуры (нужно писать код генерации)
- Нет возможности переопределить текстуру через PNG (для модов)
- Текстуры не переиспользуются (каждый раз генерируются заново)

#### 4. **Отсутствие типизации характеристик**
Нет единого интерфейса для описания блока/предмета:

```typescript
// Нет такого:
interface BlockDefinition {
  id: number;
  name: string;
  breakTime: number;
  toolType: ToolType;
  // ...
}
```

**Проблема:** Нельзя валидировать данные, легко допустить ошибку.

#### 5. **Смешение блоков и предметов**
Всё хранится в одном `BLOCK` объекте:

```typescript
export const BLOCK = {
  GRASS: 1,        // Блок
  STICK: 8,        // Предмет
  WOODEN_SWORD: 20, // Инструмент
  RAW_MEAT: 40,    // Еда
};
```

**Проблема:** Нет разделения по типам, сложно понять, что можно размещать, а что нет.

---

## 🏗️ Предлагаемая архитектура

### Структура папок

```
src/
├── registry/                    # Новая папка - реестр всех игровых объектов
│   ├── blocks/                  # Определения блоков
│   │   ├── natural/             # Природные блоки
│   │   │   ├── grass/
│   │   │   │   └── definition.ts    # Характеристики (включая цвета для процедурной генерации)
│   │   │   ├── dirt/
│   │   │   ├── stone/
│   │   │   └── ...
│   │   ├── ores/                # Руды
│   │   │   ├── coal_ore/
│   │   │   ├── iron_ore/
│   │   │   └── ...
│   │   ├── wood/                # Деревянные блоки
│   │   │   ├── wood/
│   │   │   ├── planks/
│   │   │   └── ...
│   │   └── functional/          # Функциональные блоки
│   │       ├── crafting_table/
│   │       ├── furnace/
│   │       └── ...
│   │
│   ├── items/                   # Определения предметов
│   │   ├── resources/           # Ресурсы
│   │   │   ├── stick/
│   │   │   ├── coal/
│   │   │   ├── iron_ingot/
│   │   │   └── ...
│   │   ├── food/                # Еда
│   │   │   ├── raw_meat/
│   │   │   ├── cooked_meat/
│   │   │   └── ...
│   │   └── special/             # Специальные предметы
│   │       ├── broken_compass/
│   │       └── ...
│   │
│   ├── tools/                   # Определения инструментов
│   │   ├── swords/
│   │   │   ├── wooden_sword/
│   │   │   │   └── definition.ts
│   │   │   ├── stone_sword/
│   │   │   └── iron_sword/
│   │   ├── pickaxes/
│   │   │   ├── wooden_pickaxe/
│   │   │   ├── stone_pickaxe/
│   │   │   └── iron_pickaxe/
│   │   ├── axes/
│   │   └── shovels/
│   │
│   ├── BlockRegistry.ts         # Реестр блоков
│   ├── ItemRegistry.ts          # Реестр предметов
│   ├── ToolRegistry.ts          # Реестр инструментов
│   └── types.ts                 # Общие типы
│
└── constants/
    ├── Blocks.ts                # УДАЛИТЬ (заменить на BlockRegistry)
    ├── BlockColors.ts           # УДАЛИТЬ (переместить в definitions)
    └── ...
```

**Примечание:** PNG текстуры и иконки пока не используются. Вся генерация остаётся процедурной, но архитектура готова для будущего добавления внешних текстур (для модов).

---

## 📝 Типы и интерфейсы

### Базовые типы

```typescript
// src/registry/types.ts

/**
 * Тип блока (для определения, какой инструмент эффективен)
 */
export enum BlockType {
  NONE = "none",       // Нельзя добыть
  EARTH = "earth",     // Земляные блоки (лопата)
  STONE = "stone",     // Каменные блоки (кирка)
  WOOD = "wood",       // Деревянные блоки (топор)
  PLANT = "plant",     // Растения (любой инструмент)
}

/**
 * Тип инструмента
 */
export enum ToolType {
  NONE = "none",
  PICKAXE = "pickaxe",
  AXE = "axe",
  SHOVEL = "shovel",
  SWORD = "sword",
}

/**
 * Материал инструмента
 */
export enum ToolMaterial {
  WOOD = "wood",
  STONE = "stone",
  IRON = "iron",
  DIAMOND = "diamond",  // Для будущего
}

/**
 * Физика блока
 */
export enum BlockPhysics {
  SOLID = "solid",      // Твёрдый блок (нельзя пройти)
  LIQUID = "liquid",    // Жидкость (можно пройти, замедляет)
  GAS = "gas",          // Газ (можно пройти)
  NONE = "none",        // Нет физики (воздух)
}

/**
 * Базовое определение игрового объекта
 */
export interface GameObjectDefinition {
  id: string;           // Уникальный ID (например, "grass", "wooden_sword")
  numericId: number;    // Числовой ID (для обратной совместимости)
  name: string;         // Название для UI
  description?: string; // Описание
  stackSize: number;    // Максимальный размер стака (64 по умолчанию)
  
  // Текстуры (опционально, для будущего/модов)
  texture?: string;     // Путь к PNG текстуре (если null - процедурная генерация)
  icon?: string;        // Путь к PNG иконке (если null - процедурная генерация)
}

/**
 * Определение блока
 */
export interface BlockDefinition extends GameObjectDefinition {
  type: BlockType;              // Тип блока
  physics: BlockPhysics;        // Физика блока
  
  // Характеристики ломания
  breakTime: number;            // Базовое время ломания (мс)
  requiredTool?: ToolType;      // Требуемый инструмент (если null - любой)
  minToolMaterial?: ToolMaterial; // Минимальный материал инструмента
  
  // Дроп
  drops: BlockDrop[];           // Что выпадает при ломании
  
  // Рендеринг
  colors?: {
    top: RGB;
    side: RGB;
    bottom?: RGB;
  };
  transparent?: boolean;        // Прозрачный блок (листья, стекло)
  
  // Функциональность
  isInteractable?: boolean;     // Можно ли взаимодействовать (верстак, печь)
  onInteract?: string;          // Имя функции-обработчика
}

/**
 * Дроп блока
 */
export interface BlockDrop {
  itemId: string;               // ID предмета
  count: number | [number, number]; // Количество или диапазон [min, max]
  chance?: number;              // Шанс выпадения (0.0 - 1.0, по умолчанию 1.0)
  requiresTool?: ToolType;      // Требуется инструмент для дропа
}

/**
 * Определение предмета
 */
export interface ItemDefinition extends GameObjectDefinition {
  // Еда
  isFood?: boolean;
  foodValue?: number;           // Сколько HP восстанавливает
  eatTime?: number;             // Время поедания (секунды)
  
  // Топливо
  isFuel?: boolean;
  burnTime?: number;            // Время горения (секунды)
  
  // Специальные эффекты
  onUse?: string;               // Имя функции-обработчика (для broken_compass)
}

/**
 * Определение инструмента
 */
export interface ToolDefinition extends GameObjectDefinition {
  toolType: ToolType;           // Тип инструмента
  material: ToolMaterial;       // Материал
  
  // Характеристики
  durability: number;           // Прочность (количество использований)
  speedMultiplier: number;      // Множитель скорости добычи
  damage?: number;              // Урон (для мечей)
  
  // 3D модель
  model?: string;               // Путь к 3D модели (опционально)
}

/**
 * RGB цвет
 */
export interface RGB {
  r: number; // 0.0 - 1.0
  g: number;
  b: number;
}
```

---

## 📦 Пример определения блока

### Файл: `src/registry/blocks/natural/grass/definition.ts`

```typescript
import { BlockDefinition, BlockType, BlockPhysics, ToolType, ToolMaterial } from "../../../types";

export const GRASS_BLOCK: BlockDefinition = {
  // Идентификация
  id: "grass",
  numericId: 1,
  name: "Grass Block",
  description: "A block of grass-covered dirt",
  
  // Тип и физика
  type: BlockType.EARTH,
  physics: BlockPhysics.SOLID,
  
  // Характеристики ломания
  breakTime: 750,            // Базовое время (мс)
  requiredTool: ToolType.SHOVEL, // Лучше всего лопатой
  
  // Дроп
  drops: [
    {
      itemId: "dirt",        // Выпадает земля, а не трава
      count: 1,
      chance: 1.0,
    }
  ],
  
  // Рендеринг (процедурная генерация)
  colors: {
    top: { r: 0.33, g: 0.6, b: 0.33 },   // Зелёный
    side: { r: 0.54, g: 0.27, b: 0.07 }, // Коричневый
  },
  transparent: false,
  
  // Стак
  stackSize: 64,
  
  // Текстуры (опционально, для будущего)
  // texture: "./texture.png",  // Если указано - использовать PNG вместо процедурной генерации
  // icon: "./icon.png",        // Если указано - использовать PNG для иконки инвентаря
};
```

---

## 📦 Пример определения инструмента

### Файл: `src/registry/tools/pickaxes/iron_pickaxe/definition.ts`

```typescript
import { ToolDefinition, ToolType, ToolMaterial } from "../../../types";

export const IRON_PICKAXE: ToolDefinition = {
  // Идентификация
  id: "iron_pickaxe",
  numericId: 32,
  name: "Iron Pickaxe",
  description: "A sturdy pickaxe made of iron",
  
  // Тип инструмента
  toolType: ToolType.PICKAXE,
  material: ToolMaterial.IRON,
  
  // Характеристики
  durability: 250,           // 250 использований
  speedMultiplier: 6.0,      // В 6 раз быстрее, чем рукой
  damage: 4,                 // Урон при ударе
  
  // Стак
  stackSize: 1,              // Инструменты не стакаются
  
  // Текстуры (опционально, для будущего)
  // texture: "./texture.png",  // Если указано - использовать PNG
  // icon: "./icon.png",        // Если указано - использовать PNG для иконки
  // model: "./model.json",     // Опционально: 3D модель для руки игрока
};
```

---

## 🔧 Реестры

### BlockRegistry

```typescript
// src/registry/BlockRegistry.ts

import { BlockDefinition } from "./types";
import { GRASS_BLOCK } from "./blocks/natural/grass/definition";
import { DIRT_BLOCK } from "./blocks/natural/dirt/definition";
// ... импорты всех блоков

export class BlockRegistry {
  private static blocks = new Map<string, BlockDefinition>();
  private static blocksByNumericId = new Map<number, BlockDefinition>();
  
  /**
   * Зарегистрировать блок
   */
  public static register(block: BlockDefinition): void {
    if (this.blocks.has(block.id)) {
      throw new Error(`Block with id "${block.id}" already registered`);
    }
    if (this.blocksByNumericId.has(block.numericId)) {
      throw new Error(`Block with numericId ${block.numericId} already registered`);
    }
    
    this.blocks.set(block.id, block);
    this.blocksByNumericId.set(block.numericId, block);
  }
  
  /**
   * Получить блок по ID
   */
  public static get(id: string): BlockDefinition | undefined {
    return this.blocks.get(id);
  }
  
  /**
   * Получить блок по числовому ID
   */
  public static getByNumericId(numericId: number): BlockDefinition | undefined {
    return this.blocksByNumericId.get(numericId);
  }
  
  /**
   * Получить все блоки
   */
  public static getAll(): BlockDefinition[] {
    return Array.from(this.blocks.values());
  }
  
  /**
   * Инициализировать реестр (регистрация всех блоков)
   */
  public static init(): void {
    this.register(GRASS_BLOCK);
    this.register(DIRT_BLOCK);
    // ... регистрация всех блоков
  }
}
```

---

## 🔄 Миграция

### Этап 1: Создание типов и реестров (2-3 часа)
- [x] Создать `src/registry/types.ts` с интерфейсами
- [ ] Создать `src/registry/BlockRegistry.ts`
- [ ] Создать `src/registry/ItemRegistry.ts`
- [ ] Создать `src/registry/ToolRegistry.ts`

### Этап 2: Миграция блоков (4-6 часов)
- [ ] Создать папки для блоков (`natural/`, `ores/`, `wood/`, `functional/`)
- [ ] Мигрировать определения блоков:
  - [ ] Grass, Dirt, Stone, Bedrock
  - [ ] Coal Ore, Iron Ore
  - [ ] Wood, Planks, Leaves
  - [ ] Crafting Table, Furnace
- [ ] Обновить `World.getBreakTime()` для использования реестра
- [ ] Обновить `BlockColors` для использования реестра

### Этап 3: Миграция предметов (2-3 часа)
- [ ] Создать папки для предметов (`resources/`, `food/`, `special/`)
- [ ] Мигрировать определения предметов:
  - [ ] Stick, Coal, Iron Ingot
  - [ ] Raw Meat, Cooked Meat
  - [ ] Broken Compass
- [ ] Обновить систему крафта для использования реестра

### Этап 4: Миграция инструментов (3-4 часа)
- [ ] Создать папки для инструментов (`swords/`, `pickaxes/`, `axes/`, `shovels/`)
- [ ] Мигрировать определения инструментов:
  - [ ] Wooden tools
  - [ ] Stone tools
  - [ ] Iron tools
- [ ] Обновить систему прочности инструментов

### Этап 5: Обновление системы текстур (2-3 часа)
- [ ] Обновить `TextureAtlas.ts` для использования цветов из реестра
- [ ] Обновить `ToolTextures.ts` для использования реестра
- [ ] Добавить поддержку опциональных PNG текстур (для будущего/модов):
  - [ ] Создать `TextureLoader.ts` для загрузки PNG
  - [ ] Проверка: если `texture` указан → загрузить PNG, иначе → процедурная генерация
  - [ ] Fallback на процедурную генерацию при ошибке загрузки

### Этап 6: Тестирование и отладка (2-3 часа)
- [ ] Проверить работу всех блоков
- [ ] Проверить работу всех инструментов
- [ ] Проверить систему крафта
- [ ] Проверить систему дропа

### Этап 7: Документация (1-2 часа)
- [ ] Написать гайд по добавлению новых блоков
- [ ] Написать гайд по добавлению новых инструментов
- [ ] Обновить документацию для моддинга

---

## 📈 Преимущества новой архитектуры

### 1. **Модульность**
- Каждый блок/предмет/инструмент - отдельная папка
- Все данные в одном месте
- Легко найти и изменить

### 2. **Расширяемость**
- Добавление нового блока = создание одной папки
- Не нужно редактировать множество файлов
- Поддержка модов "из коробки"

### 3. **Типобезопасность**
- Все характеристики типизированы
- Валидация на этапе компиляции
- Автодополнение в IDE

### 4. **Гибкость текстур**
- Процедурная генерация по умолчанию (как сейчас)
- Опциональная поддержка PNG изображений (для модов)
- Fallback на процедурную генерацию при отсутствии PNG
- Легко переопределить текстуры через моды

### 5. **Поддержка модов**
- Моды могут регистрировать свои блоки/предметы
- Единый API для регистрации
- Нет конфликтов ID

---

## 🎮 Пример добавления нового блока

### До (текущая система):
1. Добавить ID в `src/constants/Blocks.ts`
2. Добавить цвета в `src/constants/BlockColors.ts`
3. Добавить название в `src/constants/BlockNames.ts`
4. Добавить текстуру в `src/world/generation/TextureAtlas.ts`
5. Добавить время ломания в `src/world/World.ts`
6. Добавить рецепт в `src/crafting/Recipes.ts` (если нужно)

**Итого: 6 файлов в разных папках**

### После (новая система):
1. Создать папку `src/registry/blocks/category/my_block/`
2. Создать `definition.ts` с характеристиками (включая цвета для процедурной генерации)
3. Зарегистрировать в `BlockRegistry.init()`
4. (Опционально) Добавить `texture.png` и `icon.png` для переопределения процедурной генерации

**Итого: 1 папка, 1-3 файла в одном месте**

---

## 🔗 Связанные документы

- `doc/PROJECT_STRUCTURE.md` - Общая структура проекта
- `doc/MODDING_SDK.md` - SDK для модов (нужно обновить)
- `TODO.md` - Общий список задач

---

## 📝 Примечания

### Обратная совместимость
- Числовые ID сохраняются для совместимости с сохранениями
- Старые константы (`BLOCK.GRASS`) можно оставить как алиасы
- Постепенная миграция без поломки существующего кода

### Производительность
- Реестры используют `Map` для O(1) поиска
- Текстуры кэшируются после первой загрузки
- Нет влияния на производительность рендеринга

### Моддинг
- Моды могут регистрировать блоки через `BlockRegistry.register()`
- Процедурная генерация текстур по умолчанию (через цвета в definition)
- Опциональная загрузка PNG текстур из папки мода (если указан путь)
- Поддержка кастомных обработчиков (`onInteract`, `onUse`)

---

**Последнее обновление:** 22 января 2026
