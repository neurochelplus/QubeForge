# 🎮 Создание модов для QubeForge

Привет! Этот гайд поможет тебе создать свой первый мод для QubeForge. Никакого опыта не нужно — просто следуй инструкциям.

---

## 📦 Что такое мод?

Мод — это JavaScript файл, который добавляет новые функции в игру:
- Показывать информацию на экране (координаты, счётчики)
- Реагировать на события (разрушение блоков, урон)
- Изменять игровой мир
- И многое другое!

---

## 🚀 Создаём первый мод за 5 минут

### Шаг 1: Создай файл

Создай текстовый файл с расширением `.js`, например `my-first-mod.js`.

### Шаг 2: Скопируй шаблон

```javascript
// Это твой первый мод!
QubeForge.registerMod('my-first-mod', {
  // === ИНФОРМАЦИЯ О МОДЕ ===
  name: 'Мой первый мод',           // Название (видно в меню)
  version: '1.0.0',                  // Версия
  author: 'Твоё имя',                // Автор
  description: 'Мой крутой мод!',    // Описание
  apiVersion: '1.0',                 // Не меняй это
  permissions: ['ui.hud'],           // Какие функции нужны моду
  dependencies: [],                  // Зависимости (пока пусто)
}, {
  // === КОД МОДА ===
  init(api) {
    // Этот код выполнится при загрузке мода
    
    // Покажем текст в углу экрана
    api.ui.addHUDElement('hello', {
      position: 'top-left',
      html: '<div style="font-size: 20px;">Привет, мир! 👋</div>'
    });
  },

  onEnable() {
    // Мод включён
  },

  onDisable() {
    // Мод выключен
  },
});
```

### Шаг 3: Установи мод

1. Запусти игру
2. В главном меню нажми **"Моды"**
3. Перетащи свой `.js` файл в окно (или нажми "Выбрать файл")
4. Поставь галочку ✅ напротив мода
5. Нажми **"Применить и перезагрузить"**

### Шаг 4: Готово!

Зайди в игру — в левом верхнем углу появится "Привет, мир! 👋"

---

## 🎯 Что можно делать в модах?

### 1. Показывать текст на экране

```javascript
// Добавить элемент
api.ui.addHUDElement('my-text', {
  position: 'top-right',  // Где показать: top-left, top-right, bottom-left, bottom-right
  html: '<div>Любой текст</div>'
});

// Обновить текст
api.ui.updateHUDElement('my-text', {
  html: '<div>Новый текст!</div>'
});

// Удалить
api.ui.removeHUDElement('my-text');
```

### 2. Показывать всплывающие уведомления

```javascript
api.ui.showNotification('Ура! Что-то произошло!');
api.ui.showNotification('Это исчезнет через 5 секунд', 5000);
```

### 3. Реагировать на события

```javascript
// Когда игрок ломает блок
api.on('world:blockBreak', (event) => {
  api.ui.showNotification('Ты сломал блок!');
});

// Когда игрок ставит блок
api.on('world:blockPlace', (event) => {
  api.ui.showNotification('Блок установлен!');
});

// Когда игрок получает урон
api.on('player:damage', (event) => {
  api.ui.showNotification('Ой! Тебе больно!');
});

// Когда моб умирает
api.on('mob:death', (event) => {
  api.ui.showNotification('Моб повержен!');
});
```

### 4. Получать информацию об игроке

```javascript
const player = api.getPlayer();

// Позиция игрока
const pos = player.getPosition();
console.log(`Ты на координатах: X=${pos.x}, Y=${pos.y}, Z=${pos.z}`);

// Здоровье (нужно разрешение 'player.read')
const hp = player.getHp();      // Текущее здоровье
const maxHp = player.getMaxHp(); // Максимальное здоровье
```

### 5. Получать информацию о мире

```javascript
const world = api.getWorld();

// Какой блок на координатах? (нужно разрешение 'world.read')
const blockId = world.getBlock(10, 64, 10);

// Сейчас день или ночь?
const isDay = world.isDay();
```

### 6. Сохранять данные

```javascript
// Сохранить значение (сохраняется между сессиями!)
api.config.set('myScore', 100);

// Прочитать значение (второй параметр — значение по умолчанию)
const score = api.config.get('myScore', 0);
```

---

## 📋 Разрешения (permissions)

Мод должен указать, какие функции ему нужны. Добавь нужные в массив `permissions`:

| Разрешение | Что даёт |
|------------|----------|
| `ui.hud` | Показывать текст и уведомления на экране |
| `world.read` | Читать информацию о блоках мира |
| `world.modify` | Изменять блоки в мире |
| `player.read` | Читать здоровье и позицию игрока |
| `player.modify` | Изменять здоровье игрока |

**Пример:**
```javascript
permissions: ['ui.hud', 'world.read', 'player.read'],
```

---

## 🎓 Готовые примеры

### Счётчик разрушенных блоков

```javascript
QubeForge.registerMod('block-counter', {
  name: 'Счётчик блоков',
  version: '1.0.0',
  author: 'Ты',
  description: 'Считает сколько блоков ты сломал',
  apiVersion: '1.0',
  permissions: ['ui.hud'],
  dependencies: [],
}, {
  init(api) {
    let count = api.config.get('blocks', 0); // Загружаем сохранённое значение

    api.ui.addHUDElement('counter', {
      position: 'top-right',
      html: `<div>Сломано блоков: ${count}</div>`
    });

    api.on('world:blockBreak', () => {
      count++;
      api.config.set('blocks', count); // Сохраняем
      api.ui.updateHUDElement('counter', {
        html: `<div>Сломано блоков: ${count}</div>`
      });
    });
  },
  onEnable() {},
  onDisable() {},
});
```

### Индикатор здоровья

```javascript
QubeForge.registerMod('health-indicator', {
  name: 'Индикатор здоровья',
  version: '1.0.0',
  author: 'Ты',
  description: 'Показывает предупреждение при низком здоровье',
  apiVersion: '1.0',
  permissions: ['ui.hud', 'player.read'],
  dependencies: [],
}, {
  init(api) {
    api.ui.addHUDElement('health', {
      position: 'bottom-left',
      html: '<div style="color: green;">❤️ Здоровье в норме</div>'
    });

    api.on('player:damage', (event) => {
      const hp = event.data.newHp;
      
      if (hp <= 5) {
        api.ui.updateHUDElement('health', {
          html: '<div style="color: red;">💀 ОПАСНОСТЬ!</div>'
        });
        api.ui.showNotification('⚠️ Низкое здоровье!');
      } else if (hp <= 10) {
        api.ui.updateHUDElement('health', {
          html: '<div style="color: yellow;">⚠️ Осторожно!</div>'
        });
      } else {
        api.ui.updateHUDElement('health', {
          html: '<div style="color: green;">❤️ Здоровье в норме</div>'
        });
      }
    });
  },
  onEnable() {},
  onDisable() {},
});
```

### Координаты игрока

```javascript
QubeForge.registerMod('coords-display', {
  name: 'Координаты',
  version: '1.0.0',
  author: 'Ты',
  description: 'Показывает твои координаты',
  apiVersion: '1.0',
  permissions: ['ui.hud', 'player.read'],
  dependencies: [],
}, {
  init(api) {
    api.ui.addHUDElement('coords', {
      position: 'top-left',
      html: '<div>Загрузка...</div>'
    });

    // Обновляем каждые полсекунды
    setInterval(() => {
      const pos = api.getPlayer().getPosition();
      if (pos) {
        api.ui.updateHUDElement('coords', {
          html: `<div>X: ${Math.floor(pos.x)} Y: ${Math.floor(pos.y)} Z: ${Math.floor(pos.z)}</div>`
        });
      }
    }, 500);
  },
  onEnable() {},
  onDisable() {},
});
```

---

## ❓ Частые вопросы

**Мод не работает, что делать?**
- Открой консоль браузера (F12 → Console) и посмотри ошибки
- Проверь, что файл имеет расширение `.js`
- Убедись, что мод включён (галочка стоит)

**Как удалить мод?**
- Открой меню "Моды" и нажми 🗑️ рядом с модом

**Можно ли использовать несколько модов?**
- Да! Просто установи их все и включи нужные

**Где хранятся моды?**
- В браузере (IndexedDB). Они сохраняются даже после закрытия страницы

---

## 🆘 Нужна помощь?

Если что-то не получается — посмотри пример мода в `public/mods/example-mod.js`. Там есть рабочий код со всеми основными функциями!
