# Система реестров блоков, предметов и инструментов

**Дата создания:** 22 января 2026  
**Статус:** ✅ Реализовано и работает

---

## 📋 Содержание

1. [Обзор](#обзор)
2. [Структура](#структура)
3. [Типы и интерфейсы](#типы-и-интерфейсы)
4. [Как добавить новый блок](#как-добавить-новый-блок)
5. [Как добавить новый предмет](#как-добавить-новый-предмет)
6. [Как добавить новый инструмент](#как-добавить-новый-инструмент)
7. [API реестров](#api-реестров)
8. [Примеры](#примеры)

---

## 🎯 Обзор

Система реестров — это централизованное хранилище всех игровых объектов (блоков, предметов, инструментов). Каждый объект описывается в одном месте со всеми его характеристиками, включая текстурные паттерны.

### Преимущества

- ✅ **Все характеристики в одном месте** — не нужно редактировать 6 разных файлов
- ✅ **Типобезопасность** — TypeScript проверяет корректность данных
- ✅ **Автоматический расчёт** — время ломания, дроп, требования инструментов
- ✅ **Централизованные текстуры** — паттерны текстур хранятся в определениях блоков
- ✅ **Готовность к моддингу** — моды могут регистрировать свои объекты
- ✅ **Легко расширять** — добавление нового блока = создание одного файла

### Статистика

- **11 блоков**: природные, руды, деревянные, функциональные
- **6 предметов**: ресурсы, еда, специальные
- **12 инструментов**: мечи, кирки, топоры, лопаты (дерево/камень/железо)
- **Итого: 29 игровых объектов**

---

## 📁 Структура

```
src/registry/
├── types.ts                    # Типы и интерфейсы
├── index.ts                    # Главный файл, экспорт всего
├── BlockRegistry.ts            # Реестр блоков
├── ItemRegistry.ts             # Реестр предметов
├── ToolRegistry.ts             # Реестр инструментов
├── BreakTimeCalculator.ts      # Калькулятор времени ломания
│
├── blocks/                     # Определения блоков
│   ├── index.ts                # Экспорт всех блоков
│   ├── natural/                # Природные блоки
│   │   ├── grass.ts            # Трава
│   │   ├── dirt.ts             # Земля
│   │   ├── stone.ts            # Камень
│   │   └── bedrock.ts          # Коренная порода
│   ├── ores/                   # Руды
│   │   ├── coal_ore.ts         # Угольная руда
│   │   └── iron_ore.ts         # Железная руда
│   ├── wood/                   # Деревянные блоки
│   │   ├── wood.ts             # Дерево (ствол)
│   │   ├── planks.ts           # Доски
│   │   └── leaves.ts           # Листва
│   └── functional/             # Функциональные блоки
│       ├── crafting_table.ts   # Верстак
│       └── furnace.ts          # Печь
│
├── items/                      # Определения предметов
│   ├── index.ts                # Экспорт всех предметов
│   ├── resources/              # Ресурсы
│   │   ├── stick.ts            # Палка
│   │   ├── coal.ts             # Уголь
│   │   └── iron_ingot.ts       # Железный слиток
│   ├── food/                   # Еда
│   │   ├── raw_meat.ts         # Сырое мясо
│   │   └── cooked_meat.ts      # Жареное мясо
│   └── special/                # Специальные предметы
│       └── broken_compass.ts   # Сломанный компас
│
└── tools/                      # Определения инструментов
    ├── index.ts                # Экспорт всех инструментов
    ├── swords/                 # Мечи
    │   ├── wooden_sword.ts
    │   ├── stone_sword.ts
    │   └── iron_sword.ts
    ├── pickaxes/               # Кирки
    │   ├── wooden_pickaxe.ts
    │   ├── stone_pickaxe.ts
    │   └── iron_pickaxe.ts
    ├── axes/                   # Топоры
    │   ├── wooden_axe.ts
    │   ├── stone_axe.ts
    │   └── iron_axe.ts
    └── shovels/                # Лопаты
        ├── wooden_shovel.ts
        ├── stone_shovel.ts
        └── iron_shovel.ts
```

---

## 🔧 Типы и интерфейсы

### Перечисления

```typescript
// Тип блока (определяет, какой инструмент эффективен)
BlockType = {
  NONE: "none",       // Нельзя добыть
  EARTH: "earth",     // Земляные блоки (лопата)
  STONE: "stone",     // Каменные блоки (кирка)
  WOOD: "wood",       // Деревянные блоки (топор)
  PLANT: "plant",     // Растения (любой инструмент)
}

// Тип инструмента
ToolType = {
  NONE: "none",
  PICKAXE: "pickaxe",
  AXE: "axe",
  SHOVEL: "shovel",
  SWORD: "sword",
}

// Материал инструмента
ToolMaterial = {
  WOOD: "wood",
  STONE: "stone",
  IRON: "iron",
  DIAMOND: "diamond",  // Для будущего
}

// Физика блока
BlockPhysics = {
  SOLID: "solid",      // Твёрдый блок (нельзя пройти)
  LIQUID: "liquid",    // Жидкость (можно пройти, замедляет)
  GAS: "gas",          // Газ (можно пройти)
  NONE: "none",        // Нет физики (воздух)
}
```

### Интерфейсы

```typescript
// Текстурный паттерн для процедурной генерации
interface TexturePattern {
  pattern: string[];           // 16x16 паттерн
  colors: {
    primary: string;           // Hex цвет (#RRGGBB)
    secondary: string;         // Hex цвет (#RRGGBB)
  };
}

// Текстуры блока (для сложных блоков)
interface BlockTextures {
  top?: TexturePattern;        // Текстура верхней грани
  side?: TexturePattern;       // Текстура боковых граней
  bottom?: TexturePattern;     // Текстура нижней грани
  front?: TexturePattern;      // Текстура передней грани (для направленных блоков)
}

// Определение блока
interface BlockDefinition {
  // Идентификация
  id: string;                   // Уникальный ID ("grass", "stone")
  numericId: number;            // Числовой ID (для совместимости)
  name: string;                 // Название для UI
  description?: string;         // Описание
  
  // Тип и физика
  type: BlockType;              // Тип блока
  physics: BlockPhysics;        // Физика блока
  
  // Характеристики ломания
  breakTime: number;            // Базовое время ломания (мс)
  requiredTool?: ToolType;      // Требуемый инструмент
  minToolMaterial?: ToolMaterial; // Минимальный материал
  
  // Дроп
  drops: BlockDrop[];           // Что выпадает
  
  // Рендеринг
  // Для простых блоков - используем colors (vertex colors)
  colors?: {
    top: RGB;
    side: RGB;
    bottom?: RGB;
  };
  // Для сложных блоков - используем textures (паттерны)
  textures?: BlockTextures;
  transparent?: boolean;
  
  // Функциональность
  isInteractable?: boolean;     // Можно ли взаимодействовать (верстак, печь)
  onInteract?: string;          // Имя функции-обработчика
  isPlaceable?: boolean;        // Можно ли установить в мир (по умолчанию true для блоков)
  
  // Стак
  stackSize: number;
  
  // Текстуры (опционально, для будущего)
  texture?: string;
  icon?: string;
}

// Определение предмета
interface ItemDefinition {
  // Идентификация
  id: string;
  numericId: number;
  name: string;
  description?: string;
  
  // Еда
  isFood?: boolean;
  foodValue?: number;
  eatTime?: number;
  
  // Топливо
  isFuel?: boolean;
  burnTime?: number;
  
  // Специальные эффекты
  onUse?: string;
  
  // Рендеринг
  color?: string;
  pattern?: string[];
  
  // Стак
  stackSize: number;
}

// Определение инструмента
interface ToolDefinition {
  // Идентификация
  id: string;
  numericId: number;
  name: string;
  description?: string;
  
  // Тип
  toolType: ToolType;
  material: ToolMaterial;
  
  // Характеристики
  durability: number;
  speedMultiplier: number;
  damage?: number;
  
  // Рендеринг
  color?: string;
  pattern?: string[];
  
  // Стак
  stackSize: number;
}
```

---

## ➕ Как добавить новый блок

### Шаг 1: Создать файл определения

Создайте файл `src/registry/blocks/category/my_block.ts`:

```typescript
import type { BlockDefinition } from "../../types";
import { BlockType, BlockPhysics, ToolType } from "../../types";

export const MY_BLOCK: BlockDefinition = {
  // Идентификация
  id: "my_block",
  numericId: 100, // Уникальный числовой ID
  name: "My Block",
  description: "A custom block",

  // Тип и физика
  type: BlockType.STONE,
  physics: BlockPhysics.SOLID,

  // Характеристики ломания
  breakTime: 5000,
  requiredTool: ToolType.PICKAXE,

  // Дроп
  drops: [
    {
      itemId: "my_block",
      count: 1,
      chance: 1.0,
    },
  ],

  // Рендеринг
  colors: {
    top: { r: 0.5, g: 0.5, b: 0.5 },
    side: { r: 0.5, g: 0.5, b: 0.5 },
  },
  transparent: false,

  // Стак
  stackSize: 64,
};
```

### Шаг 2: Экспортировать из index.ts

Добавьте в `src/registry/blocks/index.ts`:

```typescript
export { MY_BLOCK } from "./category/my_block";
```

### Шаг 3: Добавить числовой ID в Blocks.ts

Добавьте в `src/constants/Blocks.ts`:

```typescript
export const BLOCK = {
  // ... существующие блоки
  MY_BLOCK: 100,
};
```

**Готово!** Блок автоматически зарегистрируется при инициализации. Больше никаких файлов редактировать не нужно!
- ✅ Ломается с правильным временем
- ✅ Учитывает тип инструмента
- ✅ Дропает правильные предметы
- ✅ Отображается с правильными цветами

---

## ➕ Как добавить новый предмет

### Шаг 1: Создать файл определения

Создайте файл `src/registry/items/category/my_item.ts`:

```typescript
import type { ItemDefinition } from "../../types";

export const MY_ITEM: ItemDefinition = {
  id: "my_item",
  numericId: 101,
  name: "My Item",
  description: "A custom item",
  
  // Если это еда:
  isFood: true,
  foodValue: 3,
  eatTime: 1.5,
  
  // Если это топливо:
  isFuel: true,
  burnTime: 20,
  
  // Рендеринг
  color: "#FF0000",
  
  stackSize: 64,
};
```

### Шаг 2: Экспортировать из index.ts

Добавьте в `src/registry/items/index.ts`:

```typescript
export { MY_ITEM } from "./category/my_item";
```

### Шаг 3: Добавить числовой ID в Blocks.ts

Добавьте в `src/constants/Blocks.ts`:

```typescript
export const BLOCK = {
  // ... существующие
  MY_ITEM: 101,
};
```

**Готово!** Предмет автоматически зарегистрируется.

---

## ➕ Как добавить новый инструмент

### Шаг 1: Создать файл определения

Создайте файл `src/registry/tools/category/my_tool.ts`:

```typescript
import type { ToolDefinition } from "../../types";
import { ToolType, ToolMaterial } from "../../types";

export const MY_TOOL: ToolDefinition = {
  id: "my_tool",
  numericId: 102,
  name: "My Tool",
  description: "A custom tool",
  
  toolType: ToolType.PICKAXE,
  material: ToolMaterial.IRON,
  
  durability: 250,
  speedMultiplier: 6.0,
  damage: 4,
  
  color: "#E0E0E0",
  stackSize: 1,
};
```

### Шаг 2: Экспортировать из index.ts

Добавьте в `src/registry/tools/index.ts`:

```typescript
export { MY_TOOL } from "./category/my_tool";
```

### Шаг 3: Добавить числовой ID в Blocks.ts

Добавьте в `src/constants/Blocks.ts`:

```typescript
export const BLOCK = {
  // ... существующие
  MY_TOOL: 102,
};
```

**Готово!** Инструмент автоматически зарегистрируется.

---

## 📚 API реестров

### BlockRegistry

```typescript
// Получить блок по строковому ID
BlockRegistry.get("grass") // → BlockDefinition | undefined

// Получить блок по числовому ID
BlockRegistry.getByNumericId(1) // → BlockDefinition | undefined

// Получить все блоки
BlockRegistry.getAll() // → BlockDefinition[]

// Проверить наличие
BlockRegistry.has("grass") // → boolean

// Количество блоков
BlockRegistry.count() // → number
```

### ItemRegistry и ToolRegistry

Аналогичный API.

### BreakTimeCalculator

```typescript
// Получить время ломания с учётом инструмента
BreakTimeCalculator.getBreakTime(blockId, toolId)
// → number (миллисекунды)

// Проверить, можно ли добыть блок (получить дроп)
BreakTimeCalculator.canHarvest(blockId, toolId)
// → boolean
```

### Проверка размещения блоков

```typescript
// Проверить, можно ли установить объект как блок
BlockRegistry.canPlaceAsBlock(numericId)
// → boolean

// Примеры:
BlockRegistry.canPlaceAsBlock(BLOCK.GRASS)      // → true (блок)
BlockRegistry.canPlaceAsBlock(BLOCK.COAL)       // → false (предмет)
BlockRegistry.canPlaceAsBlock(BLOCK.IRON_INGOT) // → false (предмет)
BlockRegistry.canPlaceAsBlock(BLOCK.WOODEN_SWORD) // → false (инструмент)
```

Эта проверка используется в `GameInitializer.ts` при размещении блоков, чтобы предотвратить установку предметов и инструментов в мир.

---

## 💡 Примеры

### Пример 1: Блок травы (простой блок с vertex colors)

```typescript
export const GRASS_BLOCK: BlockDefinition = {
  id: "grass",
  numericId: 1,
  name: "Grass Block",
  type: BlockType.EARTH,
  physics: BlockPhysics.SOLID,
  breakTime: 750,
  requiredTool: ToolType.SHOVEL,
  drops: [
    { itemId: "dirt", count: 1, chance: 1.0 }
  ],
  colors: {
    top: { r: 0.33, g: 0.6, b: 0.33 },
    side: { r: 0.54, g: 0.27, b: 0.07 },
  },
  stackSize: 64,
};
```

### Пример 2: Верстак (сложный блок с текстурными паттернами)

```typescript
const CT_TOP_PATTERN = [
  "2222222222222222",
  "2111111111111112",
  "2111111111111112",
  "2112222222222112",
  "2112112112112112",
  // ... 16 строк по 16 символов
];

export const CRAFTING_TABLE_BLOCK: BlockDefinition = {
  id: "crafting_table",
  numericId: 9,
  name: "Crafting Table",
  type: BlockType.WOOD,
  physics: BlockPhysics.SOLID,
  breakTime: 3000,
  requiredTool: ToolType.AXE,
  drops: [
    { itemId: "crafting_table", count: 1, chance: 1.0 }
  ],
  textures: {
    top: {
      pattern: CT_TOP_PATTERN,
      colors: {
        primary: "#B47850",   // Light Brown
        secondary: "#50321E", // Dark Brown
      },
    },
    side: {
      pattern: CT_SIDE_PATTERN,
      colors: {
        primary: "#B47850",
        secondary: "#50321E",
      },
    },
  },
  isInteractable: true,
  onInteract: "openCraftingTable",
  stackSize: 64,
};
```

### Пример 3: Железная кирка

```typescript
export const IRON_PICKAXE: ToolDefinition = {
  id: "iron_pickaxe",
  numericId: 32,
  name: "Iron Pickaxe",
  toolType: ToolType.PICKAXE,
  material: ToolMaterial.IRON,
  durability: 250,
  speedMultiplier: 11.0, // В 11 раз быстрее
  damage: 4,
  color: "#E0E0E0",
  stackSize: 1,
};
```

### Пример 4: Жареное мясо

```typescript
export const COOKED_MEAT_ITEM: ItemDefinition = {
  id: "cooked_meat",
  numericId: 41,
  name: "Cooked Meat",
  isFood: true,
  foodValue: 5, // Восстанавливает 5 HP
  eatTime: 1.5,
  color: "#8B4513",
  stackSize: 64,
};
```

### Пример 5: Использование в коде

```typescript
// Получить время ломания камня железной киркой
const breakTime = BreakTimeCalculator.getBreakTime(
  BLOCK.STONE,      // blockId
  BLOCK.IRON_PICKAXE // toolId
);
// → 410ms (4500 / 11)

// Проверить, можно ли добыть железную руду деревянной киркой
const canHarvest = BreakTimeCalculator.canHarvest(
  BLOCK.IRON_ORE,
  BLOCK.WOODEN_PICKAXE
);
// → false (требуется минимум каменная кирка)
```

---

## 🔄 Миграция со старой системы

### Было (6 файлов):

1. `Blocks.ts` - добавить ID
2. `BlockColors.ts` - добавить цвета
3. `BlockNames.ts` - добавить название
4. `World.ts` - добавить время ломания (switch/case)
5. `TextureAtlas.ts` - добавить текстуру
6. `Recipes.ts` - добавить рецепт

### Стало (1 файл):

```typescript
// src/registry/blocks/category/my_block.ts
export const MY_BLOCK: BlockDefinition = {
  // Всё в одном месте!
};
```

---

## 🚀 Будущие улучшения

- [ ] Поддержка PNG текстур (опциональная замена процедурной генерации)
- [ ] 3D модели для инструментов в руке игрока
- [ ] Система тегов для блоков (например, `#mineable/pickaxe`)
- [ ] Динамическая загрузка определений из JSON (для модов)
- [ ] Валидация определений при регистрации
- [ ] Hot-reload определений в dev режиме

---

## 📝 Примечания

### Обратная совместимость

- Числовые ID сохраняются для совместимости с сохранениями
- Старые константы `BLOCK.*` остаются как алиасы
- Постепенная миграция без поломки кода
- `TextureAtlas` теперь берёт паттерны из реестра вместо `BlockTextures.ts`

### Производительность

- Реестры используют `Map` для O(1) поиска
- Кэширование цветов в `BlockColors` для горячих путей
- Текстуры генерируются один раз при инициализации
- Нет влияния на производительность рендеринга

### Моддинг

- Моды могут регистрировать свои объекты через `BlockRegistry.register()`
- Автоматическая проверка конфликтов ID
- Поддержка кастомных обработчиков (`onInteract`, `onUse`)

---

## 🎨 Текстурные паттерны

### Как работают паттерны

Текстурные паттерны — это массивы из 16 строк по 16 символов, где каждый символ представляет пиксель:
- `'0'` = прозрачный пиксель
- `'1'` = основной цвет (primary)
- `'2'` = вторичный цвет (secondary)

### Пример паттерна

```typescript
const ORE_PATTERN = [
  "2222222222222222",  // Строка 0: все secondary (камень)
  "2211122222111222",  // Строка 1: вкрапления primary (руда)
  "2211122222111222",
  "2221222222212222",
  "2222222222222222",
  "2222111222222222",
  "2222111222222222",
  "2222212222222222",
  "2222222222111222",
  "2222222222111222",
  "2222222222212222",
  "2211122222222222",
  "2211122222222222",
  "2221222222222222",
  "2222222222222222",
  "2222222222222222",  // Строка 15
];
```

### Использование в блоке

```typescript
export const COAL_ORE_BLOCK: BlockDefinition = {
  // ... другие поля
  textures: {
    top: {
      pattern: ORE_PATTERN,
      colors: {
        primary: "#2A2A2A",   // Тёмно-серый (уголь)
        secondary: "#7D7D7D", // Серый (камень)
      },
    },
  },
};
```

### Когда использовать паттерны vs vertex colors

**Используй `colors` (vertex colors):**
- Для простых блоков с однородным цветом (трава, земля, камень)
- Когда нужна только базовая окраска
- Для лучшей производительности

**Используй `textures` (паттерны):**
- Для сложных блоков с деталями (верстак, печь, руды)
- Когда нужны разные текстуры для разных граней
- Для направленных блоков (печь с лицевой стороной)

---

## � Автоматическая регистрация

### Как это работает

Реестры (`BlockRegistry`, `ItemRegistry`, `ToolRegistry`) автоматически импортируют и регистрируют все объекты из соответствующих `index.ts` файлов:

```typescript
// BlockRegistry.ts
import * as AllBlocks from "./blocks/index";

public static init(): void {
  // Автоматически регистрируем все блоки
  Object.values(AllBlocks).forEach((block) => {
    if (this.isBlockDefinition(block)) {
      this.register(block);
    }
  });
}
```

### Преимущества

- ✅ **Меньше файлов для редактирования** - не нужно вручную добавлять в `Registry.init()`
- ✅ **Меньше ошибок** - невозможно забыть зарегистрировать блок
- ✅ **Проще поддерживать** - один источник истины (`blocks/index.ts`)
- ✅ **Готовность к моддингу** - моды могут добавлять свои `index.ts`

### Сравнение

**До (ручная регистрация):**
```typescript
// BlockRegistry.ts - нужно редактировать при каждом новом блоке
import { MY_BLOCK } from "./blocks/category/my_block";

public static init(): void {
  this.register(MY_BLOCK); // Легко забыть!
}
```

**После (автоматическая регистрация):**
```typescript
// blocks/index.ts - просто добавляем экспорт
export { MY_BLOCK } from "./category/my_block";

// BlockRegistry.ts - НЕ НУЖНО РЕДАКТИРОВАТЬ!
// Автоматически подхватит все экспорты из blocks/index.ts
```

---

## 🔗 Связанные документы

- [BLOCK_ITEM_ARCHITECTURE_REFACTORING.md](BLOCK_ITEM_ARCHITECTURE_REFACTORING.md) - План рефакторинга
- [PROJECT_STRUCTURE.md](PROJECT_STRUCTURE.md) - Общая структура проекта
- [MODDING_SDK.md](MODDING_SDK.md) - SDK для модов

---

**Последнее обновление:** 22 января 2026  
**Автор:** Система реестров v1.0
