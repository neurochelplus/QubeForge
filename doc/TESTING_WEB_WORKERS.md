# Тестирование Web Workers

**Дата:** 22 января 2026  
**Цель:** Проверить работу Web Workers для генерации чанков

---

## 🚀 Быстрый старт

### 1. Запустить игру

```bash
npm run dev
```

Откроется браузер на `http://localhost:5173`

---

### 2. Открыть консоль разработчика

Нажмите `F12` или `Ctrl+Shift+I`

---

### 3. Проверить инициализацию воркеров

В консоли должны появиться сообщения:
```
Worker ready (x4)
```

Это означает, что 4 воркера успешно инициализированы.

---

## 🧪 Тесты

### Тест 1: Создание нового мира

**Цель:** Проверить генерацию чанков при первом запуске

**Шаги:**
1. Нажать "New World"
2. Ввести имя мира
3. Нажать "Create"
4. Дождаться загрузки мира

**Ожидаемый результат:**
- ✅ Мир загружается плавно (без фризов)
- ✅ FPS стабильный (~60)
- ✅ Игрок спавнится на поверхности

**Проверка в консоли:**
```javascript
// Проверить статистику воркеров
game.world.chunkManager.loader.generationQueue.getWorkerStats()
// Должно вернуть: { queueSize: 0, busyWorkers: 0-4 }
```

---

### Тест 2: Движение по миру

**Цель:** Проверить динамическую загрузку чанков

**Шаги:**
1. Загрузить существующий мир
2. Двигаться в одном направлении (W или стрелка вверх)
3. Следить за FPS (показывается в правом верхнем углу)

**Ожидаемый результат:**
- ✅ FPS стабильный (~60)
- ✅ Нет фризов при загрузке новых чанков
- ✅ Чанки появляются плавно

**Проверка в консоли:**
```javascript
// Проверить количество загруженных чанков
game.world.getChunkCount()
// Должно вернуть: { visible: X, total: Y }
```

---

### Тест 3: Быстрое движение (полёт)

**Цель:** Проверить производительность при быстрой загрузке чанков

**Шаги:**
1. Включить режим полёта (F или двойной пробел)
2. Быстро лететь в одном направлении
3. Следить за FPS

**Ожидаемый результат:**
- ✅ FPS не падает ниже 50
- ✅ Нет длинных фризов (>100ms)
- ✅ Чанки загружаются быстро

---

### Тест 4: Загрузка сохранённого мира

**Цель:** Проверить загрузку чанков из IndexedDB

**Шаги:**
1. Создать новый мир
2. Походить по миру (загрузить несколько чанков)
3. Выйти в главное меню
4. Загрузить тот же мир снова

**Ожидаемый результат:**
- ✅ Мир загружается быстро
- ✅ Чанки загружаются из IndexedDB (не генерируются заново)
- ✅ FPS стабильный

**Проверка в консоли:**
```javascript
// Проверить, что чанки загружаются из IndexedDB
// (в консоли не должно быть сообщений о генерации)
```

---

## 📊 Метрики производительности

### Измерение FPS

FPS отображается в правом верхнем углу экрана.

**Целевые значения:**
- Простой (без движения): 60 FPS
- Движение (загрузка чанков): 55-60 FPS
- Быстрое движение (полёт): 50-60 FPS

---

### Измерение времени генерации

Откройте консоль и выполните:

```javascript
// Включить профилирование
window.__profiler = {
  measures: {},
  startMeasure(label) {
    this.measures[label] = performance.now();
  },
  endMeasure(label) {
    const start = this.measures[label];
    if (start) {
      const duration = performance.now() - start;
      console.log(`${label}: ${duration.toFixed(2)}ms`);
      delete this.measures[label];
    }
  }
};

// Теперь в консоли будут логи:
// chunk-generation: 15.23ms
// chunk-queue: 0.12ms
// chunk-unload: 0.45ms
// chunk-sorting: 0.23ms
```

---

### Проверка статистики воркеров

```javascript
// Получить статистику
const stats = game.world.chunkManager.loader.generationQueue.getWorkerStats();
console.log('Worker stats:', stats);

// Пример вывода:
// Worker stats: { queueSize: 2, busyWorkers: 3 }
// queueSize: количество задач в очереди
// busyWorkers: количество занятых воркеров (0-4)
```

---

## 🐛 Проверка fallback (синхронная генерация)

### Отключить воркеры

1. Открыть `src/constants/WorldConstants.ts`
2. Изменить:
   ```typescript
   USE_WEB_WORKERS: false,
   ```
3. Перезапустить игру (`npm run dev`)

**Ожидаемый результат:**
- ✅ Игра работает (fallback на синхронную генерацию)
- ❌ Фризы при генерации чанков (ожидаемо)
- ❌ FPS падает до 10-20 при генерации

---

## ✅ Чеклист тестирования

- [ ] Воркеры инициализируются (4 сообщения "Worker ready")
- [ ] Создание нового мира без фризов
- [ ] Движение по миру с стабильным FPS
- [ ] Быстрое движение (полёт) без фризов
- [ ] Загрузка сохранённого мира работает
- [ ] Статистика воркеров доступна
- [ ] Fallback на синхронную генерацию работает

---

## 🔧 Отладка

### Проблема: Воркеры не инициализируются

**Симптомы:**
- Нет сообщений "Worker ready" в консоли
- Фризы при генерации

**Решение:**
1. Проверить, что игра запущена через HTTP сервер (`npm run dev`)
2. Проверить консоль на ошибки
3. Проверить, что файл `terrain.worker.ts` собран (`dist/assets/terrain.worker-*.js`)

---

### Проблема: Фризы всё равно есть

**Возможные причины:**
1. Воркеры отключены (`USE_WEB_WORKERS: false`)
2. Слишком много чанков загружается одновременно
3. Построение меша занимает много времени (не связано с воркерами)

**Решение:**
1. Проверить `WorldConstants.USE_WEB_WORKERS` (должно быть `true`)
2. Уменьшить `CHUNK_RADIUS` (с 2 до 1)
3. Проверить профилирование (см. выше)

---

### Проблема: Чанки не загружаются

**Симптомы:**
- Игрок падает в пустоту
- Чанки не появляются

**Решение:**
1. Проверить консоль на ошибки
2. Проверить, что `waitForChunk()` работает (для спавна)
3. Проверить IndexedDB (F12 → Application → IndexedDB)

---

## 📝 Отчёт о тестировании

После тестирования заполните:

**Дата:** _____________  
**Браузер:** _____________  
**ОС:** _____________

**Результаты:**

| Тест | Результат | FPS | Примечания |
|------|-----------|-----|------------|
| Создание мира | ✅/❌ | ___ | |
| Движение | ✅/❌ | ___ | |
| Быстрое движение | ✅/❌ | ___ | |
| Загрузка мира | ✅/❌ | ___ | |
| Fallback | ✅/❌ | ___ | |

**Общая оценка:** ✅ Работает / ⚠️ Работает с проблемами / ❌ Не работает

**Комментарии:**
_____________________________________________
_____________________________________________
_____________________________________________

---

## 🔗 Связанные документы

- `doc/WEB_WORKERS_INTEGRATION.md` — документация интеграции
- `TODO_WORLD_GENERATION.md` — общий TODO лист
- `doc/README.md` — навигация по документации
