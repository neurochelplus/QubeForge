# Автоматизация текстурного атласа

**Дата:** 22 января 2026  
**Статус:** ✅ Реализовано

---

## 🎯 Цель

Полностью автоматизировать работу с текстурным атласом - добавление нового сложного блока не должно требовать редактирования `TextureAtlas.ts` или `ChunkMeshBuilder.ts`.

---

## 📊 Было vs Стало

### До (ручная работа)

При добавлении нового сложного блока нужно было редактировать **5 файлов**:

1. ✏️ Создать `src/registry/blocks/my_block.ts` с паттерном
2. ✏️ Экспортировать в `src/registry/blocks/index.ts`
3. ✏️ **Добавить в `TextureAtlas.applyBlockTextures()`** ❌
4. ✏️ **Добавить в `ChunkMeshBuilder.getUVCoords()`** ❌
5. ✏️ Добавить ID в `src/constants/Blocks.ts`

```typescript
// TextureAtlas.ts - НУЖНО БЫЛО РЕДАКТИРОВАТЬ!
const myBlock = BlockRegistry.get("my_block");
if (myBlock?.textures?.top) {
  this.applyPattern(data, 176, 192, myBlock.textures.top); // Slot 11
}

// ChunkMeshBuilder.ts - НУЖНО БЫЛО РЕДАКТИРОВАТЬ!
else if (type === BLOCK.MY_BLOCK) slot = 11;
```

### После (автоматизация)

При добавлении нового сложного блока нужно редактировать **3 файла**:

1. ✏️ Создать `src/registry/blocks/my_block.ts` с паттерном
2. ✏️ Экспортировать в `src/registry/blocks/index.ts`
3. ✏️ Добавить ID в `src/constants/Blocks.ts`

**Всё остальное происходит автоматически!** ✅

---

## 🔧 Как это работает

### 1. TextureAtlas автоматически находит блоки с текстурами

```typescript
private static applyBlockTexturesFromRegistry(data: Uint8Array): void {
  const allBlocks = BlockRegistry.getAll();

  for (const block of allBlocks) {
    if (!block.textures) continue; // Пропускаем простые блоки

    // Автоматически выделяем слоты для каждой грани
    const slots = {};
    if (block.textures.top) {
      slots.top = this.allocateSlot(); // Следующий свободный слот
      this.applyPattern(data, slots.top, block.textures.top);
    }
    // ... аналогично для side, bottom, front

    // Сохраняем маппинг: numericId → слоты
    this.slotMap.set(block.numericId, slots);
  }
}
```

### 2. ChunkMeshBuilder автоматически получает слоты

```typescript
private getUVCoords(type: number, side: string, ...): { u0: number; u1: number } {
  // Автоматически получить слот из TextureAtlas
  const slot = TextureAtlas.getSlot(type, side);
  
  return {
    u0: uvStep * slot + uvInset,
    u1: uvStep * (slot + 1) - uvInset,
  };
}
```

### 3. Динамическое выделение слотов

```typescript
private static nextSlot = 1; // Slot 0 зарезервирован для шума

private static allocateSlot(): number {
  if (this.nextSlot >= this.SLOT_COUNT) {
    throw new Error(`No more slots available (max ${this.SLOT_COUNT})`);
  }
  return this.nextSlot++;
}
```

---

## 📝 Примеры

### Простой блок (vertex colors)

```typescript
// src/registry/blocks/natural/sand.ts
export const SAND_BLOCK: BlockDefinition = {
  id: "sand",
  numericId: 15,
  name: "Sand",
  type: BlockType.EARTH,
  
  // Простой блок - используем colors
  colors: {
    top: { r: 0.93, g: 0.87, b: 0.69 },
    side: { r: 0.93, g: 0.87, b: 0.69 },
  },
  
  // ... остальные поля
};
```

**Результат:**
- Использует Slot 0 (базовый шум)
- Цвет из vertex colors
- Никаких изменений в TextureAtlas или ChunkMeshBuilder

### Сложный блок (текстурный паттерн)

```typescript
// src/registry/blocks/functional/chest.ts
const CHEST_FRONT_PATTERN = [
  "2222222222222222",
  "2111111111111112",
  // ... 16 строк
];

export const CHEST_BLOCK: BlockDefinition = {
  id: "chest",
  numericId: 16,
  name: "Chest",
  type: BlockType.WOOD,
  
  // Сложный блок - используем textures
  textures: {
    front: {
      pattern: CHEST_FRONT_PATTERN,
      colors: {
        primary: "#8B4513",
        secondary: "#654321",
      },
    },
    side: {
      pattern: CHEST_SIDE_PATTERN,
      colors: { ... },
    },
  },
  
  // ... остальные поля
};
```

**Результат:**
- TextureAtlas автоматически выделяет 2 слота (front, side)
- Рисует паттерны в эти слоты
- ChunkMeshBuilder автоматически использует правильные слоты
- Никаких изменений в коде!

---

## 🎨 Текущее распределение слотов

При запуске игры в консоли появится:

```
Initializing BlockRegistry...
BlockRegistry initialized with 11 blocks
TextureAtlas: Block "leaves" (6) → slots: { top: 1 }
TextureAtlas: Block "planks" (7) → slots: { top: 2 }
TextureAtlas: Block "crafting_table" (9) → slots: { top: 3, side: 4, bottom: 5 }
TextureAtlas: Block "coal_ore" (10) → slots: { top: 6 }
TextureAtlas: Block "iron_ore" (11) → slots: { top: 7 }
TextureAtlas: Block "furnace" (14) → slots: { front: 8, side: 9, top: 10 }
TextureAtlas: Generated with 11 slots used
```

**Свободные слоты:** 11 (slot 11 доступен для новых блоков)

---

## 🚀 Преимущества

1. **Меньше файлов для редактирования** - не нужно трогать TextureAtlas или ChunkMeshBuilder
2. **Невозможно забыть** добавить блок в атлас - всё автоматически
3. **Меньше ошибок** - нет ручного назначения слотов
4. **Проще для новичков** - просто создай определение блока
5. **Готовность к моддингу** - моды могут добавлять блоки без конфликтов слотов

---

## ⚠️ Ограничения

### Максимум 12 слотов

Атлас имеет размер 192x16px = 12 слотов по 16x16px.

- **Slot 0:** Зарезервирован для базового шума (простые блоки)
- **Slots 1-11:** Доступны для сложных блоков (11 слотов)

Если нужно больше слотов:
1. Увеличить размер атласа (например, 192x32px = 24 слота)
2. Или использовать несколько атласов

### Специальная обработка

Некоторые блоки требуют специальной обработки:
- **Листья:** Автоматическая прозрачность (40% пикселей)
- **Руды:** Шум для каменного фона (secondary color = #7D7D7D)
- **Печь:** Ротация (front грань зависит от направления)

---

## 🔗 Связанные документы

- [REGISTRY_SYSTEM.md](REGISTRY_SYSTEM.md) - Система реестров
- [TEXTURE_ATLAS_MIGRATION.md](TEXTURE_ATLAS_MIGRATION.md) - Миграция паттернов в реестр
- [AUTO_REGISTRATION.md](AUTO_REGISTRATION.md) - Автоматическая регистрация

---

**Последнее обновление:** 22 января 2026
