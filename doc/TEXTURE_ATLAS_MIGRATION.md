# Миграция TextureAtlas в систему реестров

**Дата:** 22 января 2026  
**Статус:** ✅ Завершено

---

## 🎯 Цель

Полностью интегрировать текстурные паттерны в систему реестров, устранив дублирование данных между `BlockTextures.ts` и определениями блоков.

---

## 📋 Что было сделано

### 1. Обновлены типы реестра

Добавлены новые интерфейсы в `src/registry/types.ts`:

```typescript
// Текстурный паттерн (16x16 символов)
interface TexturePattern {
  pattern: string[];           // '0' = прозрачный, '1' = primary, '2' = secondary
  colors: {
    primary: string;           // Hex цвет
    secondary: string;         // Hex цвет
  };
}

// Текстуры блока
interface BlockTextures {
  top?: TexturePattern;
  side?: TexturePattern;
  bottom?: TexturePattern;
  front?: TexturePattern;      // Для направленных блоков (печь)
}
```

### 2. Обновлены определения блоков

Добавлены текстурные паттерны в:
- ✅ `src/registry/blocks/functional/crafting_table.ts` - 3 паттерна (top, side, bottom)
- ✅ `src/registry/blocks/functional/furnace.ts` - 3 паттерна (front, side, top)
- ✅ `src/registry/blocks/ores/coal_ore.ts` - 1 паттерн (ore pattern)
- ✅ `src/registry/blocks/ores/iron_ore.ts` - 1 паттерн (ore pattern)
- ✅ `src/registry/blocks/wood/planks.ts` - 1 паттерн (planks pattern)

### 3. Переписан TextureAtlas

`src/world/generation/TextureAtlas.ts` теперь:
- ✅ Берёт паттерны из `BlockRegistry` вместо `BlockTextures.ts`
- ✅ Использует метод `applyBlockTextures()` для применения паттернов из реестра
- ✅ Поддерживает специальную обработку руд (с шумом для камня)
- ✅ Экспортирует `hexToRgb()` как публичный метод

### 4. Обновлены зависимости

- ✅ `src/player/PlayerHand.ts` - использует `TextureAtlas.hexToRgb()`
- ✅ `src/constants/ToolTextures.ts` - берёт паттерны из `BlockRegistry`

### 5. Удалены устаревшие файлы

- ❌ `src/constants/BlockTextures.ts` - удалён
- ❌ `src/constants/textures/BlockPatterns.ts` - удалён

### 6. Обновлена документация

- ✅ `doc/REGISTRY_SYSTEM.md` - добавлен раздел о текстурных паттернах
- ✅ `doc/PROJECT_STRUCTURE.md` - обновлены ссылки

---

## 🎨 Как это работает

### Простые блоки (vertex colors)

```typescript
export const GRASS_BLOCK: BlockDefinition = {
  // ...
  colors: {
    top: { r: 0.33, g: 0.6, b: 0.33 },
    side: { r: 0.54, g: 0.27, b: 0.07 },
  },
};
```

Используются для:
- Трава, земля, камень, дерево
- Однородные цвета без деталей
- Лучшая производительность

### Сложные блоки (текстурные паттерны)

```typescript
export const CRAFTING_TABLE_BLOCK: BlockDefinition = {
  // ...
  textures: {
    top: {
      pattern: CT_TOP_PATTERN,  // 16x16 символов
      colors: {
        primary: "#B47850",     // Светло-коричневый
        secondary: "#50321E",   // Тёмно-коричневый
      },
    },
  },
};
```

Используются для:
- Верстак, печь, руды, доски
- Сложные детали и узоры
- Разные текстуры для разных граней

---

## 📊 Результаты

### До миграции

```
src/constants/BlockTextures.ts     - Паттерны верстака, печи, руд
src/constants/textures/BlockPatterns.ts - Определения паттернов
src/registry/blocks/...            - Определения блоков БЕЗ паттернов
src/world/generation/TextureAtlas.ts - Импортирует BlockTextures
```

**Проблема:** Дублирование данных, паттерны отдельно от определений блоков

### После миграции

```
src/registry/blocks/...            - Определения блоков С паттернами
src/world/generation/TextureAtlas.ts - Берёт паттерны из BlockRegistry
```

**Результат:** 
- ✅ Все данные блока в одном месте
- ✅ Нет дублирования
- ✅ Легче добавлять новые блоки
- ✅ Готовность к моддингу

---

## 🚀 Преимущества

1. **Централизация данных**
   - Все характеристики блока (включая текстуры) в одном файле
   - Не нужно редактировать несколько файлов при добавлении блока

2. **Типобезопасность**
   - TypeScript проверяет корректность паттернов
   - Автодополнение в IDE

3. **Готовность к моддингу**
   - Моды могут регистрировать блоки с кастомными паттернами
   - Единый API для всех блоков

4. **Упрощение кода**
   - `TextureAtlas` стал проще и понятнее
   - Меньше файлов для поддержки

---

## 📝 Примечания

### Обратная совместимость

- Сохранения не затронуты (используются числовые ID)
- Рендеринг работает идентично
- Нет изменений в производительности

### Будущие улучшения

- [ ] Поддержка PNG текстур (опциональная замена паттернов)
- [ ] Динамическая загрузка паттернов из JSON (для модов)
- [ ] Редактор паттернов в dev режиме

---

## 🔗 Связанные документы

- [REGISTRY_SYSTEM.md](REGISTRY_SYSTEM.md) - Полная документация системы реестров
- [BLOCK_ITEM_ARCHITECTURE_REFACTORING.md](BLOCK_ITEM_ARCHITECTURE_REFACTORING.md) - План рефакторинга

---

**Последнее обновление:** 22 января 2026
